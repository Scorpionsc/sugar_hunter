import palette from "../../palette";

export default {
    container: {
        flex: 1,
        backgroundColor: palette.color1,
        alignItems: 'center',
        justifyContent: 'center',
    }
};